# crontab 样例（实际由 deploy/setup.sh 写入 /etc/crontab，secret 占位由 deploy.sh 替换；
# 本文件仅作参考/手工恢复用）。
# __SECRET__ 换成 Setting.cron_secret 的值（生产库里 seed 生成的那份），
# 备份脚本本体在 /opt/checkmate/backup.sh（由 setup.sh 写入，逻辑见 setup.sh 第 5 步）。
# 应用监听 0.0.0.0:3210，cron 走本机回环访问即可。
# remind 每小时整点打点：到不到提醒小时由应用里的 Setting.remind_hour 决定
#（管理员在 /admin 改，即时生效，无需改 crontab）。
# weekly 定在周一 01:10：补卡窗口开到次日北京 01:00，结算必须等窗口关上再跑。
0 * * * * root curl -s "http://127.0.0.1:3210/api/cron/remind?secret=__SECRET__" >/dev/null
10 1 * * 1 root curl -s "http://127.0.0.1:3210/api/cron/weekly?secret=__SECRET__" >/dev/null
30 3 * * * root curl -s "http://127.0.0.1:3210/api/cron/cleanup?secret=__SECRET__" >/dev/null
0 4 * * * root /opt/checkmate/backup.sh >> /var/lib/checkmate/backups/backup.log 2>&1
